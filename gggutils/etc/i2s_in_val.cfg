[Sites]
    [[__many__]]
        slices = boolean
        no_date_dir = boolean(default=False)
        slices_in_subdir = boolean

        [[[__many__]]]
            slices = boolean(default=None)
            no_date_dir = boolean(default=None)
            slices_in_subdir = boolean(default=None)
